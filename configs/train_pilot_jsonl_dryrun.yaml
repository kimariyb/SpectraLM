# SpectraLM lazy-JSONL dry run configuration
# Validates dataset loading and message construction without loading a VLM.

model_path: /Users/kimariyb/d/models/modelscope/Qwen3-VL-4B-Instruct
max_seq_length: 4096
load_in_4bit: true

dataset_dir: dataset/paired_jsonl_pilot_50k
dataset_backend: lazy_jsonl
eval_split_name: validation
target_format: smiles
h_snr: 500.0
c_snr: 300.0
render_seed: 3407
image_size: [224, 224]
max_train_samples: 8
max_eval_samples: 4

dry_run: true
output_dir: outputs/pilot-jsonl-dryrun
per_device_train_batch_size: 1
gradient_accumulation_steps: 1
lora_r: 16
lora_alpha: 32
num_train_epochs: 1
max_steps: 1
learning_rate: 2.0e-4
weight_decay: 0.01
warmup_steps: 0
logging_steps: 1
eval_steps: 1
save_steps: 1
seed: 3407
